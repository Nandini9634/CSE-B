class Solution {
public:
    int reverse(int x) {
        string s=to_string(x);
        int n=s.size();
        int i=0;
        int temp;
        for(int i = 0; i < n / 2; i++) 
            swap(s[i], s[n - i - 1]); 
        int x1=stoi(s);
        return x1;
    }
};
